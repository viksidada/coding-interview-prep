const twoChar = (str) => {

    let mySet = new Set();

    for (let char of str) {
        mySet.add(char);
    }

    for (let char of mySet) {
        for()
    }

}

console.log(twoChar('viksidada'));